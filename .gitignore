# Per-checkout lane assignment — never commit (see docs/LANE.md.example)
docs/LANE.md

# dependencies
/node_modules
/.pnp
.pnp.*

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files — secrets live here, never in git
.env
.env*.local
.env*
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# prisma — the client is generated by `postinstall`, not committed
/prisma/*.db
/src/generated
